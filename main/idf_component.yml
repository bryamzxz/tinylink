# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Bryam (bryamzxz)
#
# M1 (control plane) uses HTTP/1.1 inside the Noise channel for the single
# /machine/register round-trip — see components/tinylink/src/ts2021_client.h
# for the scope note. The HTTP/2 dependency (espressif/nghttp) returns in
# M2 when MapResponse needs a long-lived stream.
#
# WireGuard data plane (droscy/esp_wireguard) also returns in M2.

dependencies:
  idf:
    version: ">=5.5,<5.6"
